# Docker: backend only needs dist/ + node_modules/ + assets
# Frontend is built separately

# VCS
.git
.gitignore

# Python (legacy)
.venv
venv
__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info
pip-log.txt

# IDE
.vscode
.idea
*.swp
*.swo

# Runtime data
sessions
coverage
.nyc_output
o11y
workspace

# Frontend source (not needed in backend image)
frontend/node_modules
frontend/.next

# Env files (passed via docker-compose)
.env
.env.local
frontend/.env.local

# Docker files
docker-compose.yml
Dockerfile
frontend/Dockerfile

# Docs
DEPLOY.md
README.md
