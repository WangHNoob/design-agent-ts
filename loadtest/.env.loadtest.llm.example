# 复制说明到根 .env 后重启 backend — 真实 LLM query 压测（勿提交真实 Key）
# 必须使用真实 LLM_API_KEY（来自你的私有 .env，不要写进本文件）
AGENT_FRAMEWORK=langgraph
HITL_ENABLED=false
ALLOW_EMAIL_PASSWORD=true
COST_ENABLED=true
MQ_ENABLED=true
# 建议保持与生产相近的槽位/RPM，便于观察排队与限流
# MAX_CONCURRENT_PER_USER=3
# COST_RPM_LIMIT_PER_USER=60
